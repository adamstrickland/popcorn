/quuxs:
  get:
    description: placeholder
    tags:
      - nonsense
    responses:
      200:
        description: list of quuxs
        schema:
          type: array
          items:
            $ref: '#/definitions/quux'
/quuxs/{id}:
  get:
    description: a single quux
    tags:
      - nonsense
    responses:
      200:
        description: the details of the quux
        schema:
          $ref: '#/definitions/quux'
